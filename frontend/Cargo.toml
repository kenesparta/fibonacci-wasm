[package]
name = "frontend"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
wasm-bindgen = "0.2.45"
shared = { path = "../shared" }

[package.metadata.wasm-pack.profile.release]
wasm-opt = false
